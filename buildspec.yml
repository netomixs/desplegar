version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 16
    commands:
      - cd site
      - npm install
      - npm i -g serverless
  build:
    commands:
      - serverless deploy

